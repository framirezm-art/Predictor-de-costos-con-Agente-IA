# Usado por docker-compose.yml para inyectar el secreto al contenedor backend.
# Copia este archivo a ".env" en la raíz del repo y completa el valor real.
NVIDIA_API_KEY=
NVIDIA_MODEL=deepseek-ai/deepseek-v4-flash
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
